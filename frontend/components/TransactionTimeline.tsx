"use client"; const STEPS = ["Created", "Signed", "Submitted", "Confirmed"]; interface Props { currentStep: number; timestamps?: Record<number, string>; } export default function TransactionTimeline({ currentStep, timestamps }: Props) { return ( <div className="py-4"> <h4 className="text-sm font-semibold text-white mb-4">Status</h4> <div className="relative"> {STEPS.map((label, i) => { const done = i <= currentStep; const active = i === currentStep; return ( <div key={label} className="flex items-start gap-3 mb-4 last:mb-0"> <div className={`flex-shrink-0 w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold ${done ? "bg-stellar-500 text-white" : "bg-white/10 text-slate-400"}`}>{done ? "?" : i + 1}</div> <div className="flex-1 min-w-0"> <p className={`text-sm font-medium ${done ? "text-white" : "text-slate-400"}`}>{label}</p> {timestamps?.[i] && <p className="text-xs text-slate-500 mt-0.5">{new Date(timestamps[i]).toLocaleTimeString()}</p>} </div> </div> ); })} </div> </div> ); }
